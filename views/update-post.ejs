<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head', { title: 'Update Student' }) %>
    <link rel="stylesheet" href="/styles/forms.css" />
  </head>

  <body>
    <%- include('includes/header') %>
    <main>
      <h1>Update Students</h1>
      <form action="/all-students/<%= post.id %>/edit" method="POST">
        <div class="form-control">
          <label for="roll">Roll Number</label>
          <input
            type="text"
            id="roll"
            value="<%= post.roll %>"
            name="roll"
            required
          />
        </div>
        <div class="form-control">
          <label for="name">Name</label>
          <input
            type="text"
            id="name"
            value="<%= post.name %>"
            name="name"
            required
            maxlength="255"
          />
        </div>
        <div class="form-control">
          <label for="cgpa">CGPA</label>
          <textarea id="cgpa" name="cgpa" required rows="5"><%= post.cgpa %></textarea
          >
        </div>
        <button class="btn">Update Student</button>
      </form>
    </main>
  </body>
</html>
